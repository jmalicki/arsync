# Implementation Plans

This directory contains implementation plans generated by `/implementation-plan`.

Plans follow the naming convention:
- `FEATURE_NAME.md` - matches the corresponding design doc in `docs/designs/`

Each plan includes:
- Phased approach with checkboxes
- Quality checks using slash commands
- Test requirements
- PR preparation steps

